<li id='post-<%=i._id%>'>
    <p>
        <%=i.content%> <a class="like-button" id="like-<%=i._id%>" data-likes="<%=i.likes.length%>" href="/likes/toggle/?id=<%=i.id%>&type=Post"> - <%=i.likes.length%> Like</a>   <i class="fa-solid fa-thumbs-up"></i> <br>
            - <%=i.user.name%>
                <% if(locals.user && locals.user.id==i.user.id) {%>
                    <a class="delete-post-button" id="<%=i._id%>" href="/posts/destroy/<%=i.id%>"> - Delete Post</a>
                    <%}%>
                        <%if(locals.user){%>
                            <form action='comments/create' id="new-comment-form-<%=i._id%>" method="post">
                                <textarea name="content" cols="20" rows="1" placeholder="Add a Comment...."
                                    required></textarea>
                                <input type="hidden" name="post" value="<%=i._id%>">
                                <input type="submit" value="Comment">
                            </form>
                            <%}%>
                                <div id="comment-div-container">
                                    <ul id='post-comments-<%=i._id%>'>
                                        <%for(let j of i.comments){%>

                                            <%- include('_comments',{j:j}) -%>
                                                <%}%>
                                    </ul>

                                </div>

    </p>

</li>